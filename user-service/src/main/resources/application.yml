spring:
  datasource:
    url: jdbc:postgresql://localhost:5437/user-service
    username: userserviceadmin
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
